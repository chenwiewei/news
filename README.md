About THIS PROJECT
use macos`s command `say` to make audio,
then use ImageMagick to make a video cover,
finnally use ffmpeg to merge video and audio.

and you can auto upload the video to bilibili, just install `biliup`

